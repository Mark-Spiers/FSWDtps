import { EmployeePerDepartment } from './employee-per-department';

describe('EmployeePerDepartment', () => {
  it('should create an instance', () => {
    expect(new EmployeePerDepartment()).toBeTruthy();
  });
});
