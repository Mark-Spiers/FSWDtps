import { NinuniqueDirective } from './ninunique.directive';

describe('NinuniqueDirective', () => {
  it('should create an instance', () => {
    const directive = new NinuniqueDirective();
    expect(directive).toBeTruthy();
  });
});
